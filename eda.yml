- name: Event driven ansible test
  host: localhost

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:
    - name: Test
      condition: event.something == "something"
      action:
        debug:
          msg: "rule is triggered"
